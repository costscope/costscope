global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: 'costscope'
    metrics_path: /metrics
    static_configs:
      - targets: ['costscope:8080']
