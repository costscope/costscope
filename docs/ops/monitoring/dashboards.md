# Dashboards

This file links to canonical Grafana dashboards and documents dashboard conventions and panels.

- Location of dashboard JSON examples: `monitoring/grafana/` (in repo root)
- Naming convention: `svc-<service>-overviewsvc-<service>-databasesvc-<service>-alerts`

```bash
,
```

Include sample screenshots and field mappings when available.
