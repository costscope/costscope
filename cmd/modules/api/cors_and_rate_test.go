package api

import "testing"

// minimal build test: ensures package test files compile and run in CI
func TestCorsAndRate_Smoke(t *testing.T) {
	// intentionally empty; other tests exercise behavior
}
