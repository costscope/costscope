## Example environment file for `act`
# Copy to .github/act.env and adjust. Unset lines are safe.

GO_VERSION=1.21
COSTSCOPE_INVARIANTS_TOLERANCE=0.01
PARITY_TOLERANCE=1e-9
INVARIANTS_TOLERANCE=0.01
PERF_BENCH_DURATION_MAX=1.15
PERF_BENCH_ALLOC_MAX=1.20

# Pin CI tools image tag used by containerized jobs.
# Examples:
#   CI_IMAGE_TAG=2025.10.10-abcdef12
#   CI_IMAGE_TAG=sha256:deadbeef...   # when using digest form (see notes below)
#   CI_IMAGE_TAG=dev-local            # for local development images
CI_IMAGE_TAG=dev-local
