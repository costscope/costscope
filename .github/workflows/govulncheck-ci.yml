name: govulncheck CI (full scan)

env:
  GO_VERSION: "1.24.x"
  GOVULNCHECK_VERSION: latest

on:
  pull_request:
  workflow_dispatch:
  push:
    # Temporary: run on push; will migrate some to tags later
    branches: [main]

jobs:
  noop-on-push:
    if: ${{ github.event_name == 'push' }}
    name: noop on push
    runs-on: ubuntu-latest
    steps:
      - name: Noop summary
        run: |
          echo '### govulncheck CI (noop on push)' >> "$GITHUB_STEP_SUMMARY"
          echo 'Full scan runs on PRs, schedule, or manual dispatch only.' >> "$GITHUB_STEP_SUMMARY"
  noop-act:
    if: ${{ github.event_name != 'push' && github.actor == 'nektos/act' }}
    name: act fallback
    runs-on: ubuntu-latest
    steps:
      - name: Explain act path
        run: |
          echo "This full govulncheck job targets a self-hosted runner; under act we run a lightweight fallback."
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8
      - name: Run govulncheck via script (act)
        run: |
          mkdir -p govuln_reports
          export GOVULNCHECK_OUT=govuln_reports/ci_full_scan.json
          export GOVULNCHECK_PKGS=./...
          export GOVULNCHECK_TIMEOUT=1200
          bash scripts/security/run-govulncheck.sh || true
          ls -lh govuln_reports/ci_full_scan.json || true
      - name: Summarize report (act)
        env:
          INPUT: govuln_reports/ci_full_scan.json
        run: |
          bash scripts/security/summarize-govulncheck.sh
      - name: Store artifact locally (act)
        run: |
          echo "ACT detected; skipping upload-artifact (requires ACTIONS_RUNTIME_TOKEN)."

  resolve-ci-image:
    if: ${{ github.event_name != 'push' && github.actor != 'nektos/act' }}
    name: Resolve CI tools image
    runs-on: ubuntu-latest
    outputs:
      ci_tools_image: ${{ steps.out.outputs.image }}
    env:
      CI_IMAGE_REPO: ghcr.io/costscope/ci-base
      CI_TOOLS_IMAGE: ${{ vars.CI_TOOLS_IMAGE }}
      CI_IMAGE_TAG: ${{ vars.CI_IMAGE_TAG }}
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8
      - id: out
        run: bash ./scripts/ci/resolve-ci-image.sh
  govulncheck:
    name: Full govulncheck and upload report (self-hosted large-memory)
    # Skip on push and under nektos/act locally
    if: ${{ github.event_name != 'push' && github.actor != 'nektos/act' }}
    # This job is intended to run on a runner with >=16 GiB RAM.
    # Configure a self-hosted runner with label `large-memory` and the job
    # will run there. If you prefer GitHub-hosted runners, change `runs-on`
    # to `ubuntu-22.04`, but be aware hosted runners may not have >=16 GiB RAM.
    runs-on: [self-hosted, linux, x64, large-memory]
    needs: resolve-ci-image
    container: ${{ needs.resolve-ci-image.outputs.ci_tools_image }}
    timeout-minutes: 180

    steps:
      - name: Checkout
        # pinned actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 — source: https://github.com/actions/checkout/commits/main (2025-09-28)
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8

      - name: Detect act environment
        id: detect
        shell: bash
        run: |
          if [[ "${ACT:-}" == "true" || "${IS_ACT:-}" == "true" ]]; then
            echo "is_act=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_act=false" >> "$GITHUB_OUTPUT"
          fi

      - name: "Sanity: go version"
        run: go version

      - name: Verify govulncheck availability
        run: govulncheck -version || true

      - name: Run full govulncheck (module)
        env:
          # allow govulncheck more time and constrain CPU/GOGC to reduce memory
          GOVULNCHECK_TIMEOUT: 7200
          GOMAXPROCS: 2
          GOGC: 100
        run: |
          mkdir -p govuln_reports
          export GOVULNCHECK_OUT=govuln_reports/ci_full_scan.json
          export GOVULNCHECK_PKGS=./...
          bash scripts/security/run-govulncheck.sh || true
          ls -lh govuln_reports/ci_full_scan.json || true

      - name: Summarize govulncheck report
        env:
          INPUT: govuln_reports/ci_full_scan.json
          # FAIL_ON_VULNS: "true" # enable to gate CI on findings
        run: |
          bash scripts/security/summarize-govulncheck.sh

      - name: Upload govulncheck JSON report
        if: ${{ steps.detect.outputs.is_act == 'false' }}
        # pinned actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 — source: https://github.com/actions/upload-artifact/commits/main (2025-09-28)
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: govulncheck-full-report
          path: govuln_reports/ci_full_scan.json

      - name: Summarize top advisory IDs and post comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd
        with:
          script: |
            const fs = require('fs');
            const core = require('@actions/core');
            const path = 'govuln_reports/ci_full_scan.json';
            if (!fs.existsSync(path)) {
              core.info('govulncheck report not found');
            } else {
              const data = fs.readFileSync(path,'utf8');
              // extract unique GO- IDs
              const ids = new Set();
              const re = /"id":\s*"(GO-[0-9-]+)"/g;
              let m;
              while ((m = re.exec(data)) !== null) ids.add(m[1]);
              const arr = Array.from(ids).slice(0,100);
              const body = `govulncheck completed on runner with labels: ${process.env['RUNNER_LABELS'] || ''}\n\nFound ${ids.size} unique advisory IDs (showing up to 100):\n\n${arr.join('\n')}`;
              github.rest.issues.createComment({owner: context.repo.owner, repo: context.repo.repo, issue_number: context.payload.pull_request.number, body});
            }
