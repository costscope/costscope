//go:build ignore
// +build ignore

// Canonical validation and estimation live as methods on GCPConverter in converter.go.
// The prior standalone functions were duplicated and are intentionally removed.
package gcp
