// Package common hosts shared helpers for mapping/normalization/classification
// used across provider converters. It must not import provider-specific code.
// Package note: provider-specific converters are split into subpackages for clarity and to avoid import cycles.
package common
