package config

import "testing"

// TestConfigPlaceholder exists so the package retains a test after legacy config service removal.
func TestConfigPlaceholder(t *testing.T) {
	// Intentionally empty.
}
