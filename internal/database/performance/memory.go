package performance

// Legacy memory manager removed – definitions inlined in engine.go. File retained intentionally
// (empty) to avoid broad git churn while refactor stabilizes. Will be deleted in a follow-up phase.
